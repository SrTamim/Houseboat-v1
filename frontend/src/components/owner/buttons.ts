/* ────────────────────────────────────────────────────────────
   Owner console button class strings.

   These now live in ./styles alongside the rest of the owner design
   system; this module re-exports them so existing
   `import { BTN_B } from '@/components/owner/buttons'` sites keep working.
   ──────────────────────────────────────────────────────────── */

export { BTN, BTN_SM, BTN_B, BTN_O, BTN_OK, BTN_DANGER } from './styles';
