# Where the browser sends /api requests. In dev, next.config rewrites
# /api/* to this. In prod (Vercel), point at the Railway API URL.
API_PROXY_TARGET=http://localhost:4000

# Public base used by client code when NOT going through the rewrite proxy.
NEXT_PUBLIC_API_URL=/api
